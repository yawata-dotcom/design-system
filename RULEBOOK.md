# 全社共通デザイン・ルールブック（社長確定・必読）

このルールは**全プロダクト共通**。同じ社員が複数プロダクト（交通誘導警備＝Esecurity／人材事業＝Esupport 等）を使うため、見た目だけでなく**操作・言葉・入力の作法まで統一**する。**変更できるのは社長のみ。** 各製品は「使うだけ」。

> **このファイルが"唯一の正（source of truth）"。** 各製品の人・AIは、UI/デザインの作業前に**必ずこの最新版を読む**こと。ローカルの記載と食い違ったら、**この公開版が優先**。
> ・ライブ参照（常に最新）：`https://raw.githubusercontent.com/yawata-dotcom/design-system/main/RULEBOOK.md`
> ・見本帳（標準部品）：`https://yawata-dotcom.github.io/design-system/gallery.html`
> ・色トークン：`https://raw.githubusercontent.com/yawata-dotcom/design-system/main/tokens.css`
> ・**技術・設計方針**（フロント/バック/DB/API/テストの全社方針）：`https://raw.githubusercontent.com/yawata-dotcom/design-system/main/TECH-POLICY.md`
> 社長がこのファイルを1か所更新すれば、全製品に効く（各製品へ個別配布は不要）。

## 1. 色（公式カラートークン）
- 色は `tokens.css` の**17トークンだけ**を使う。**直書きの色（独自hex）は禁止**。
- CSSは `var(--token)`、SVGの `fill`/`stroke` はトークンの実hex。
- 意味：緑=OK／赤=警告／オレンジ=注意／青=基調・リンク／青緑=分類アクセント。同じ色＝同じ意味。

## 2. 操作の作法（挙動）
- **保存の仕方を揃える**：入力途中は自動保存（下書き）／**外部送信・状態変更だけ明示ボタン＋確認ダイアログ**／完了はできるだけ自動判定。
- **開閉はスライド方式**（クラス切替＋max-height transition 0.7s）。即時表示やdetailsは使わない。
- ダイアログ・トーストの出し方を揃える。

## 3. 言葉（用語）
- 同じ意味に同じ言葉。「確定／登録／保存」「削除／取り消し」を混ぜない。用語集に従う（増えたらここに追記）。

## 4. 画面の骨格・入口
- サイドメニュー位置・上部バー・ボタンの置き場所・**共通の入口（ランチャー）**を揃える。
- ページ構成パターン（見出し→絞り込み→一覧→詳細）を踏襲。
- **ヘッダー（共通上バー）は固定の「正典セット」（2026-07-19 社長確定・全プロダクト共通の絶対ルール。正＝警備リポ `design/ヘッダー統一_壁打ち確定_20260719.md`）**：**会社名(.company) / 役割セレクタ(.rolesw>select) / メールアドレス / ログアウト(button.appshell-logout) / ヘルプ**（**5点全部この順で右寄せ**＝2026-07-20社長確認で「左端=会社名」を訂正）。製品が要素を省略・改変しない。**旧5点の「最終同期」「期間」は廃止**（2026-07-19社長決定＝残っていたら番人が赤）。
  - **会社名＝運営会社の正式名だけ**：警備=`株式会社Esecurity`／他=`株式会社Esupport`。**事業名サフィックス（「（人材事業）」等）は禁止**＝どの事業かは左上ブランド（正式プロダクト名）が担う。
  - **役割セレクタ＝そのプロダクトで付与されている役割名（番号なし）**。**役割未整備の事業は役割を作らない・配らない**＝部品がグレーの「準備中」（disabled）を描く（フェイルクローズド＝権限の先渡し禁止。役割構成はその事業の実装時に壁打ち→社長が明示的に付与）。
  - **認証の表示＝「メールアドレス＋ログアウト」**（氏名は出さない＝どのアカウントで入っているかが確実）。**ログアウトボタンの形＝見本帳①ボタンの「淡・小」**（白地・青枠・青太字・角丸8px）を**共有部品が描く**＝製品側でCSSを書かない・上書きしない。SSO未接続の製品（例：建設）は接続時にこの形で追加する。※ポータル（入口・別様式）は構成の対象外だが、**ログアウトボタンの形だけは同じ淡ボタン（小）に合わせる**。
  - **Reactは共有 `<AppShell>`（v0.5.0〜）に値を注入するだけ**（`topbar={{ company, role?, authEmail?, onLogout? }}`）。枠・並び・アイコン・ボタンは部品が必ず描く＝**構造のズレは型（props）＝ビルドで防がれる**。各製品が自前でヘッダーを組み直さない。
  - **自動チェック（2026-06-22〜・2026-07-20改）**：`tools/check-header.sh`（静的HTMLで正典セットの存在＋製品独自操作の混入＋**廃止要素「最終同期」「期間」の消し忘れ**を検査）＋実測番人 `check-icon-color-runtime.mjs`（実画面に正典セットが実在するかを照合）。モックとアプリのヘッダー食い違いを機械で止める。
- **左上のブランド（サイドバー上部＝共有 `<AppShell>` の `brand`）＝そのプロダクトの正式名を表示する**（2026-07-16 社長確定）。正式名は事業切替ランチャーの項目と**一字一句同じ**にそろえる：`本社管理`（これだけ「事業」を付けない）／`建設事業`／`人材事業`／`交通誘導警備事業`／`施設保安警備事業`／`宿泊事業`／`不動産事業`。
  - **「事業」を落とした略称・独自のキャッチコピー・2行表記にしない**（例：警備の「交通誘導警備業務／総合管理システム」2行は不可＝`交通誘導警備事業` の1行に統一）。
  - **ログイン画面は別物**＝全社共通の1枚（「Esupport総合管理システム／株式会社Esupport｜社内業務システム」）のままで、左上ブランドとは別（§7 ログイン・権限）。混同しない。
  - システム全体の正式名称＝**Esupport総合管理システム**（「総合」であり「統合」ではない）。

## 5. 入力・データの形式
- 日付・金額（¥・カンマ）・氏名（姓名2分割）・必須の赤バッジ・エラー表示を揃える。

## 6. アイコン（freee公式採用＝Material Design Icons のみ・これ以外禁止）
- **使ってよいアイコンは、見本帳「印 アイコン」に載っている freee公式採用の Material Design Icons（react-icons の `md`）だけ。** 絵文字・他のアイコンセット（FontAwesome 等）・自作SVG・画像（png/jpg）アイコンは**禁止**。freee vibes はこれ以外のアイコンを認めていない。
- **見本帳に無いアイコンが必要なときは、勝手に足さず社長に確認**（§9と同じ運用。社長が見本帳へ追加 → 配布）。
- 実体ファイルは `icons/<名前>.svg`（色は親要素の `color:var(--token)` を継ぐ＝`fill="currentColor"`）。
- **色の継ぎ方（重要・塗りを固定しない）**：アイコンは **`fill:currentColor`** で親の文字色（＝トークン色）を継ぐ。これで「選択中メニュー＝青(`--fblue`)／ヘッダー＝グレー(`--gray`)」が自動でそろう。
  - **Reactは `react-icons/md` を直接 `<Icon className="ic">`（またはインラインSVG）で描く。** 基底に `.ic{ fill:currentColor; stroke:none; … }` を置く（正典 `shell-demo.html` と同一）。
  - **`@freee_jp/vibes` の `MaterialIcon` は使わない。** これは塗りを `#323232` に固定して `currentColor` を継がず、選択中／ヘッダーの色が濃グレーにズレる（2026-06-22 建設で実際に発生）。`#323232` も `md` も“公式”のため、色番人・アイコン番人を素通りしてしまうので、人の目でなく機械で止める。
  - アイコンSVGの塗りは `fill="currentColor"` か `fill="var(--token)"`。**直書きhex（`fill="#…"`）は禁止**（ランチャー等のトークン色も `var(--token)` で）。
- 同じ記号＝同じ意味（チェック＝完了 等）。勝手に別アイコンで同概念を表さない。
- **自動チェック**：CI（design-check）で2本走る。`tools/check-icons.sh`（2026-06-21〜）＝Material以外（他react-iconsセット・lucide/heroicons/fontawesome等・Lucide系スプライト痕跡）を検出。`tools/check-icon-color.sh`（2026-06-22〜）＝`MaterialIcon`の使用・`.ic`が`fill:currentColor`を継がない/固定塗り・アイコンSVGの直書きhex を検出。さらに**実測番人（Playwright）**で「選択中=青／ヘッダー=グレー」を実画面のcomputed colorで照合（AppShellを持つ製品）。※絵文字や手書きSVGの真贋は機械判定が難しいため、これら自動チェック＋見本帳＋レビューで守る。

## 7. ログイン・権限
- **同じ社員が複数製品を使う**前提。将来は**共通ログイン（Googleサインイン）＋権限で製品を出し分け**。各製品はこれに合わせて作る。

## 8. アクセシビリティ・動き
- 読みやすい文字サイズ・コントラストの最低基準を満たす。アニメ速度は揃える（0.7s）。
- ※「高齢者向けの“大きい”文字」は交通誘導警備の隊員スマホ固有の要件で、全社共通ルールではない。

## 9. 共通部品の扱い（重要・全員＝人もAIも厳守）
- **UIは必ず「見本帳（gallery）」の標準部品から作る**。見本帳に無い部品・独自の“似て非なるもの”を勝手に作らない。
  - 見本帳：**https://yawata-dotcom.github.io/design-system/gallery.html** （標準部品＋コピー用コード）
- **作りたいものに合う部品が見本帳に無い時は、勝手に発明せず手を止めて社長に確認**。社長が「①見本帳へ昇格して新標準にする／②この画面だけの個別部品にする」を判断 → 昇格分は配布。各製品は使うだけ。
- **画面のレイアウト（部品の並べ方）は壁打ちで設計してよい**＝新しい画面はOK。ただし**材料（部品・色・作法）は必ず標準から**。色は§1の17トークンのみ（CIが直書きhexを機械的にはじく）。
- これは**人の開発者もAI（Claude Code等）も同じ**。各製品のCLAUDE.mdにも明記し、徹底する。
- **公式部品ガード（CI強制・2026-06-23〜）**：公式部品へ「置換済み」の“独自版クラス”（例：`btn-p`/`cmp`/`mkv` 等）を**マークアップ（`class=`/`className=`）で使うと CI が赤で停止**する（`tools/check-components.sh`）。＝**公式部品があるのに独自で作り直す逸脱**を機械的に防ぐ仕組み。CSS定義（`.x{}`）は対象外。社長承認済みの製品固有部品は対象外。全製品（esecurity／portal／人材事業／本リポ）に展開済み。

## 10. セキュリティ・個人情報（全製品共通）
- **実個人情報・実書類・鍵/トークンをリポに入れない**。開発はダミー/マスクデータのみ。実データは社内完結。
- 本番デプロイ・本番の鍵は社長が持つ。

## 11. 重なり順（z-index）：重ねて出す要素は最前面（厳守）
サブメニュー（飛び出すメニュー）・ドロップダウン・ポップオーバー・ツールチップ・**アプリ/事業切替ランチャー**など「**重ねて出す要素**」は、**必ずヘッダーや他の固定要素より前面**に出す。隠れて操作できない不具合（再発しやすい）を防ぐため。
- 開くときに **`document.body` 直下へ出す（ポータル化）** ＋ **`position:fixed`** ＋ **`z-index` をヘッダーより十分大きく（例：2000以上）**。
- **祖先の `overflow`（hidden/auto）・`transform`・`position:sticky`（例：くっつくサイドバー）でクリップ／スタッキングが頭打ち**にならないようにする。`position:fixed` でも、祖先が作る「重なりの入れ物（スタッキングコンテキスト）」に閉じ込められると、いくら `z-index` を大きくしても本文の後ろに沈む。だから**必ず body 直下へ出す**のが安全。
- **全サブメニュー共通の1ルール**にして再発防止（箇所ごとに個別対応しない）。
- ヘッダー等の固定要素は低めの z-index（例：5〜50）に保ち、重ねUIは必ずその上。

---
更新履歴：
- 2026-06-15 初版（公式トークン確定・コラボ環境構築に伴い作成）。
- 2026-06-15 §11「重なり順：サブメニューは最前面」を追加（サイドメニューのサブメニューがヘッダーに隠れる不具合の再発防止・社長指示で共通ルール化）。
- 2026-06-15 §11 を補強：対象に「アプリ/事業切替ランチャー」を明記。`position:sticky` のサイドバーも“重なりの入れ物”を作り `position:fixed` を閉じ込めると追記（esecurity の左上ランチャーが本文の後ろに隠れる不具合→body 直下へポータル化して解決）。
- 2026-06-15 §9 を補強：「**UIは必ず見本帳の標準部品から作る／無い部品は勝手に作らず社長に確認**」を明文化（見本帳URL併記・人もAIも全員対象・各製品CLAUDE.mdにも明記して徹底）。社長指示。
- 2026-06-19 §6 を確定：**アイコンは freee公式採用の Material Design Icons のみ・これ以外（絵文字/他セット/自作SVG/画像）禁止**。見本帳に「印 アイコン」セクション（代表128個・検索＋コピー）と実体ファイル `icons/*.svg` を追加。社長指示。
- 2026-06-22 §6 を補強：**アイコンは `fill:currentColor` で文字色（トークン）を継ぐ。塗りを固定する `MaterialIcon` は使わない**（react-icons/md を `<Icon className="ic">` で直接描画）。建設で「選択中=青のはずが濃グレー #323232」事故が発生し、色番人・アイコン番人を素通りしたため、静的番人 `tools/check-icon-color.sh` ＋ 実測番人（Playwright）を全製品に追加。社長指示。
- 2026-06-22 §4 を補強：**ヘッダーは固定の正典5点セット（最終同期/期間/会社/役割セレクタ/ヘルプ）＋認証スロット**。警備①のReactヘッダーがモックとズレ（最終同期・期間・役割セレクタ・ヘルプ欠落）、check-headerがmockのみ検査で見逃したため、**共有 `<AppShell>` にヘッダー構造を固定（topbarを必須propsに）**＋ check-header をReact対象にも拡張＋実測番人に5点存在チェックを追加。社長指示。
- 2026-06-23 §9 を補強：**公式部品ガード（`tools/check-components.sh`）で、公式へ置換済みの“独自版クラス”の使用を CI 強制**（`btn-p`/`cmp`/`mkv` 等をマークアップで使うと赤で停止）。「公式があるのに独自で作り直す逸脱」だけを機械的に防ぐ（CSS定義・社長承認済みの製品固有部品は対象外）。全製品（esecurity／portal／人材事業／本リポ）へ展開。社長指示。
- 2026-07-16 §4 を補強：**左上のブランド（共有 `<AppShell>` の `brand`）＝そのプロダクトの正式名を表示する**（事業切替ランチャー7項目と一字一句一致・「事業」を落とさない・警備の2行表記「交通誘導警備業務／総合管理システム」は `交通誘導警備事業` の1行に統一）。ログイン画面の全社共通表示（Esupport総合管理システム）とは別物。社長確定（キャプチャーで正式名称を提示）。
